---
all:
  children:
    workstation:
      hosts:
        localhost:
          ansible_connection: local

    rhel_managed:
      children:
        rhel_sandbox:
          hosts:
            rhel10-test01:
              ansible_connection: ssh
